<p style="color: green"><%= notice %></p>

<h1><%= Project.model_name.human.pluralize %></h1>

<div id="projects">
  <% @projects.each do |project| %>
    <%= render project %>
    <p>
      <%= link_to "Show this project", project %>
    </p>
  <% end %>
</div>

<%= link_to "New project", new_project_path %>
